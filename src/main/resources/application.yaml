spring:
  application:
    name: store
  datasource:
    url: jdbc:postgresql://localhost:5433/storedb
    username: fara
    password: password
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}